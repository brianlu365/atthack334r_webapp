<div id="leaderboard">
  <h1>leaderboard</h1>  
  <table class="table">
    <tr>
      <th>name</th>
      <th>points</th>
    </tr>
    <% @users.each do |u| %>
      <tr>
        <td><%= u.name %></td>
        <td><%= u.points %></td>
      </tr>
    <% end %>
  </table>
</div>
